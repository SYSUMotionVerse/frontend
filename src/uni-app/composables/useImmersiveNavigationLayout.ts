/**
 * Build compatibility marker for WeChat DevTools incremental preview.
 *
 * The former layout composable has been removed. This inert export keeps its
 * generated module path available while older preview indexes are still cached.
 */
export const IMMERSIVE_NAVIGATION_LAYOUT_COMPAT_VERSION = 'restored-flow-v1'
